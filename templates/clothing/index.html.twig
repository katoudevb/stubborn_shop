{% extends 'base.html.twig' %}

{% block title %}Nos Vêtements
{% endblock %}

{% block body %}
	<h1 class="page-title">Nos Vêtements</h1>

	<div class="clothing-grid">
		{% for clothing in clothes %}
			<div class="clothing-card {% if clothing.highlighted %}highlighted{% endif %}">
				{% if clothing.image %}
					<img src="{{ asset('images/clothes/' ~ clothing.image) }}" alt="{{ clothing.name }}" class="clothing-image">
				{% endif %}
				<div class="clothing-info">
					<h2>{{ clothing.name }}</h2>
					<p class="price">{{ clothing.price }}
						€</p>
					{% if clothing.highlighted %}
						<span class="badge">🌟 En vedette</span>
					{% endif %}
				</div>
			</div>
		{% else %}
			<p>Aucun vêtement disponible.</p>
		{% endfor %}
	</div>

	<style>
		.page-title {
			text-align: center;
			margin-bottom: 30px;
			font-size: 2rem;
			color: #333;
		}

		/* Grille responsive */
		.clothing-grid {
			display: grid;
			grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
			gap: 20px;
			padding: 0 20px;
		}

		/* Carte vêtement */
		.clothing-card {
			border: 1px solid #ddd;
			border-radius: 12px;
			overflow: hidden;
			background-color: #fff;
			transition: transform 0.3s, box-shadow 0.3s;
			position: relative;
		}

		.clothing-card:hover {
			transform: translateY(-5px);
			box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
		}

		/* Vêtement en vedette */
		.clothing-card.highlighted {
			border: 2px solid #f39c12;
		}

		.clothing-image {
			width: 100%;
			height: 220px;
			object-fit: cover;
		}

		.clothing-info {
			padding: 15px;
			text-align: center;
		}

		.clothing-info h2 {
			font-size: 1.2rem;
			margin: 10px 0 5px;
			color: #333;
		}

		.price {
			font-weight: bold;
			color: #555;
		}

		.badge {
			display: inline-block;
			margin-top: 8px;
			padding: 3px 8px;
			background-color: #f39c12;
			color: #fff;
			font-size: 0.8rem;
			border-radius: 5px;
		}
	</style>

{% endblock %}
